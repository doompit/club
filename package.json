{
  "name": "doompify-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "backend",
    "bot"
  ],
  "scripts": {
    "start:backend": "npm start --workspace backend",
    "migrate": "npm run migrate --workspace backend",
    "start:bot": "npm start --workspace bot",
    "register": "npm run register --workspace bot"
  },
  "engines": {
    "node": ">=18"
  }
}
